Spring boot project completed for the self directed course at NSCC.

## Setup instructions

Run the below command to build the project and create the database docker container.

```
./mvnw spring-boot:run
```
